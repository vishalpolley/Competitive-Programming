#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("in.in", "r", stdin);
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string s;
    cin >> s;
    int ch[26];
    memset(ch, 0, )
    for(int i=0; i<s.size(); ++i)
        s[i]
    return 0;
}
