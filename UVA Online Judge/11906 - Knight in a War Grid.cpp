#include<bits/stdc++.h>
using namespace std;

int main()
{
    freopen("input.txt", "r", stdin);
    int t, r, c, m, n, w, posx, posy;
    char mov;
    cin >> t;
    while(t--)
    {
        cin >> r >> c >> m >> n;
        char** arr = new char*[r];
        for(int i=0; i<r; i++)
            arr[i] = new char[c];
        cin >> w;
        for(int i=0; i<w; i++)
        {
            cin >> x >> y;
            arr[x][y] = '#';
        }
        posx
        while(posx!=0 && posy!=0)
        {
            if(arr[])
        }
        for(int i=0; i<r; i++)
            delete [] arr[i];
        delete [] arr;
    }
    return 0;
}
