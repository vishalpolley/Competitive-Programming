#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("in.in", "r", stdin);
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int P, S;
    cin >> P >> S;
    int SC[S], NS[S];
    for(int i=0; i<P; ++i)
    {
        for(int a=0; a<S; ++a)
            cin >> SC[a];
        for(int b=0; b<S; ++b)
            cin >> NS[b];
        for(int j=0; j<S; ++j)
        {
            if()
        }
    }
}
