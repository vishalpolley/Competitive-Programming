#include <bits/stdc++.h>
using namespace std;

void stringPermutation(string str)
{

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t; cin >> t;
    while(t--)
    {
        string str; cin >> str;
        stringPermutation(str);
        cout << "\n";
    }
    return 0;
}
